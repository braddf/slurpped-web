version: '3'
services:
  postgres:
    image: postgres:latest
    environment:
      POSTGRES_PASSWORD: test_password
      POSTGRES_USER: postgres
      POSTGRES_DB: groentetas
    ports:
      - "5432:5432"
